/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Mantén cualquier otra configuración que tengas aquí
  images: {
    domains: ['localhost', '127.0.0.1'],
  },
};

export default nextConfig;